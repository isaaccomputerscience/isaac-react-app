# User-agent: * means the rules apply to all web crawlers.
# Disallow: /private/ prevents crawlers from accessing the /private/ directory.
# Allow: / allows crawlers to access everything else.

# Routes below always redirect an unauthenticated request (crawlers included)
# to /login, so there is no indexable content behind them — see the
# "Page with redirect" Search Console issue for isaaccomputerscience.org.
#
# Query-string variants of /concepts/ and /topics/ (e.g. ?examBoard=,
# ?stage=, ?topic=) self-canonicalize to the bare path — see
# CanonicalHrefElement.tsx — so crawling them wastes crawl budget on known
# duplicates. See the "Alternative page with proper canonical tag" Search
# Console issue for isaaccomputerscience.org.
User-agent: *
Disallow: /questions/
Disallow: /tests
Disallow: /account
Disallow: /assignment/
Disallow: /assignments
Disallow: /progress
Disallow: /my_gameboards
Disallow: /my_markbook
Disallow: /gameboard_builder
Disallow: /set_assignments
Disallow: /teacher_account_request
Disallow: /login
Disallow: /concepts/*?
Disallow: /topics/*?

# Block a specific crawler
User-agent: AdsBot-Google
Disallow:

# Allow Googlebot access to everything else
User-agent: Googlebot
User-agent: Bingbot
Disallow: /questions/
Disallow: /tests
Disallow: /account
Disallow: /assignment/
Disallow: /assignments
Disallow: /progress
Disallow: /my_gameboards
Disallow: /my_markbook
Disallow: /gameboard_builder
Disallow: /set_assignments
Disallow: /teacher_account_request
Disallow: /login
Disallow: /concepts/*?
Disallow: /topics/*?

# Sitemap location
Sitemap: https://isaaccomputerscience.org/sitemap.xml